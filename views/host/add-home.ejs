<%- include('../partials/head') %>
<body class="bg-gray-50 min-h-screen">
    <%- include('../partials/header') %>
    <%- include('../partials/nav') %>

    <main class="max-w-3xl mx-auto mt-10 px-4">
        <h1 class="text-3xl font-bold text-center mb-6">List Your Property</h1>
        <p class="text-gray-600 text-center mb-8">Fill in the details below to add your property to our listings.</p>
        
        <% if (locals.error) { %>
            <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-6">
                <%= error %>
            </div>
        <% } %>
        
        <form action="/host/add-home" method="POST" enctype="multipart/form-data" class="bg-white p-8 rounded-lg shadow-md">
            <div class="mb-6">
                <label for="houseName" class="block text-gray-700 font-semibold mb-2">Property Name</label>
                <input type="text" id="houseName" name="houseName" placeholder="E.g., Cozy Beach House" required 
                    value="<%= locals.formData ? formData.houseName : '' %>"
                    class="w-full p-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500" />
            </div>
            
            <div class="mb-6">   
                <label for="Location" class="block text-gray-700 font-semibold mb-2">Location</label>
                <input type="text" id="Location" name="Location" placeholder="E.g., Miami, Florida" required 
                    value="<%= locals.formData ? formData.Location : '' %>"
                    class="w-full p-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500" />
            </div>
            
            <div class="mb-6">   
                <label for="ppn" class="block text-gray-700 font-semibold mb-2">Price Per Night (₹)</label>
                <input type="number" id="ppn" name="ppn" placeholder="E.g., 5000" required min="1" step="1"
                    class="w-full p-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500" />
                <p class="text-gray-500 text-sm mt-1">Enter a whole number (no decimals)</p>
            </div>
            
            <div class="mb-8">
                <label for="photo" class="block text-gray-700 font-semibold mb-2">Upload photo</label>
                <input type="file"accept="image/*" id="photo" name="photo" required 
                    class="w-full p-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500" />
                <p class="text-gray-500 text-sm mt-1">Provide an image of your property</p>
            </div>
            
            <div class="flex justify-end">
                <button type="submit" class="bg-blue-600 hover:bg-blue-700 text-white py-3 px-6 rounded-lg transition duration-300">
                    List Property
                </button>
            </div>
        </form>
        
        <div class="text-center mt-6">
            <a href="/host/host-home-list" class="text-blue-500 hover:underline">View my listed properties</a>
        </div>
    </main>
</body>
</html>